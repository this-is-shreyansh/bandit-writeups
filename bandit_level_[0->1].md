# Bandit Level [0] -> Level 1

- **Date:** 2026-08-10
- **Target:** Accessing password for the next level from `Readme.md`

---

### 1. Objective:

to locate and access `readme.md` using linux commands and access passwords for the next level

### 2. Commands used:

`ls`: lists files and directories in the current working directory
`cat`: used to read the contents of the file.

### 3. Methodology/Steps:

- using the `ls` command we check the  current directory locating the `readme.md` that is present in it.
- next we use `cat` to read the document.

### 4. Syntax:

`ls` : lists the contents of the directory

`cat readme.md`: prints the content of the file 
